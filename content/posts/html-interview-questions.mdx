---
title: "Html Interview Questions"
thumbnail: "/images/posts/html-interview-questions.png"
date: "2023-10-27"
description: "Commonly asked HTML interview questions for web developers."
---

## What are Semantic and non-semantic tags in HTML?

Semantic HTML tags are tags that define the meaning of the content they contain. For example, tags like `<header>`, `<article>`, and `<footer>` are semantic HTML tags. They clearly indicate the role of the content they contain. On the other hand, tags like `<div>` and `<span>` are typical examples of non-semantic HTML elements.

## What are empty tags in HTML?

An empty element is a component that doesn't have any embedded elements or text elements. Empty tags contain only the opening tag but they perform some action in the webpage. For example, `<br>` tag is an empty tag.

## What will happen if we don't pass doctype?

If you don't declare the doctype, browsers can interpret the document as something else other than HTML and switch into quirks mode, which is, basically, a mode for documents with no doctype declaration. There can be a lot of quirks in the quirks mode; it can pretty much mess up how your document appears on the screen!

## What are the differences between `<div>` and `<span>` elements in HTML?

Span and div are both generic HTML elements that group together related parts of a web page. However, they serve different functions. A div element is used for block-level organization and styling of page elements, whereas a span element is used for inline organization and styling.

## Name any 5 Tags which are include in HTML5 and its application

- `<article>`
- `<nav>`
- `<figure>`
- `<aside>`
- `<section>`
